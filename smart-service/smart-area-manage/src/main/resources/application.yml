server:
  port: 8080

security:
  oauth2:
    resource:
      jwt:
        key-value: mlnx

org:
  shan:
    security:
      app:
        jwtSigneKey: mlnx     # jwt 加密的签名秘钥

logging:
  level:
    org.shan: debug
    com.mlnx: debug
    org.springframework.security: info

spring:
  datasource:
    url: jdbc:mysql://192.168.1.134:3306/smart_hospital_area?useUnicode:true&characterEncoding:utf8
    username: root
    password:
    driver-class-name: com.mysql.jdbc.Driver
